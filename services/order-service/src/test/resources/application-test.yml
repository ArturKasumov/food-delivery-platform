spring:
  kafka:
    consumer:
      group-id: test-order-service
    listener:
      auto-startup: false

app:
  kafka:
    topics:
      order-events: test.orders.events
      payment-events: test.payments.events

  environment: test
