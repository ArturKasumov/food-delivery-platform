<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context"
                    name="applicationName"
                    source="spring.application.name"
                    defaultValue="unknown-service"/>
    <springProperty scope="context"
                    name="environment"
                    source="app.environment"
                    defaultValue="local"/>
    <springProperty scope="context"
                    name="logPath"
                    source="logging.file.path"
                    defaultValue="logs"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSS} [%thread] %-5level [%X{correlationId:-no-correlation-id}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logPath}/${applicationName}.json</file>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <mdcKeyFieldName>trace_id=traceId</mdcKeyFieldName>
            <mdcKeyFieldName>span_id=spanId</mdcKeyFieldName>
            <customFields>{"service_name":"${applicationName}","environment":"${environment}"}</customFields>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${logPath}/archive/${applicationName}.%d{yyyy-MM-dd}.%i.json.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <logger name="org.hibernate.SQL" level="ERROR"/>
    <logger name="org.hibernate.type" level="ERROR"/>
    <logger name="org.springframework.http" level="ERROR"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="JSON_FILE"/>
    </root>
</configuration>
